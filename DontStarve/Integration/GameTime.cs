namespace DontStarve.Integration;

internal static class GameTime {
    internal const long TICKS_PER_TIMESCALE = 438;
    internal const long TICKS_PER_HOUR = 6 * TICKS_PER_TIMESCALE;
    internal const long TICKS_PER_DAY = 24 * 6 * TICKS_PER_TIMESCALE;
}